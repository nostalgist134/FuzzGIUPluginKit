package main

import "C"
import (
	"bytes"
	"encoding/binary"
	"unsafe"
)
/* CUSTOM IMPORTS */

/* CODE */

//export PluginWrapper
func PluginWrapper(payload string, /* FORMAL PARAMETERS */) uintptr {
	s := /* FUN_NAME */(payload, /* ACTUAL PARAMETERS */)
	buffer := bytes.Buffer{}
	binary.Write(&buffer, binary.LittleEndian, int32(len(s)))
	buffer.WriteString(s)
	return uintptr(unsafe.Pointer(&buffer.Bytes()[0]))
}

func main() {}