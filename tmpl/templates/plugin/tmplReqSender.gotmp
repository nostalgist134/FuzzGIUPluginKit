package main

import (
	"encoding/binary"
	"encoding/json"
	"unsafe"
)
/* CUSTOM IMPORTS */

/* CODE */

func PluginWrapper(/* FORMAL PARAMETERS */) ([]byte, error) {
	sendMetaJson := args[0].([]byte)
	sendMeta := new(fuzzTypes.SendMeta)
	err := json.Unmarshal(sendMetaJson, sendMeta)
	if err != nil {
	    return nil, err
	}

	resp := /* FUN_NAME */(sendMeta)
	return json.Marshal(resp)
}

func main() {}