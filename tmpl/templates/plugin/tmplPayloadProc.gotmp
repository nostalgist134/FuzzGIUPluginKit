package main

import "unsafe"
/* CUSTOM IMPORTS */

/* CODE */

func PluginWrapper(payload string) uintptr {
    s := /* FUN_NAME */(payload)
    heapStr := new(string)
    *heapStr = s
    return uintptr(unsafe.Pointer(heapStr))
}

func main() {}