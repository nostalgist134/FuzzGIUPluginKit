package main

import (
	"encoding/binary"
	"encoding/json"
	"unsafe"
)
/* CUSTOM IMPORTS */

/* CODE */

func PluginWrapper(/* FORMAL PARAMETERS */) ([]byte, error) {
	fuzzJson := args[0].([]byte)
	fuzz := new(fuzzTypes.Fuzz)

	err := json.Unmarshal(fuzzJson, fuzz)
	if err != nil {
	    return nil, err
	}

	newFuzz := /* FUN_NAME */(fuzz, /* ACTUAL PARAMETERS */)
	return json.Marshal(newFuzz)
}

func main() {}